#include"Incremental.h"

Incremental::Incremental(std::string graphFilename, unsigned vertexesNumber) : AbstractGraph(graphFilename, vertexesNumber)  {
	
}

std::vector<unsigned> Incremental::getCriticalPath() {
	//todo
}